1.如果服务性能的增长与资源的增加成比例，服务就是可扩展的；性能问题，对于单个用户来说是缓慢的；扩展性问题，单个用户较快但在高负载下会变慢；
2.延迟是执行操作或计算花费的时间，吞吐量是单位时间内操作或运算的数量，通常以可接受延迟下的最大吞吐量为目标；
3.CAP理论，一致性，可用性，分区容错性；网络不可靠，分区容错性必须，需要在可用性和一致性做选择；原子业务的用CP更合适，最终一致的业务用AP更合适；支持可用性
  一般通过故障切换（主备切换，流量管控切换）和复制（主从复制，主主复制）；
4.DNS路由流量：加权轮询调度；基于延迟路由；基于地理位置路由；
